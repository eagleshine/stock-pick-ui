<!--
<div class="layout-column">
{{#each model as |ticker|}}
  <div class="layout-row flex-xs flex-gt-xs-50">
    {{#paper-card as |card|}}
      {{#card.title as |title|}}
        {{#title.text as |text|}}
          {{#text.headline}}
            {{ticker.symbol}}
          {{/text.headline}}
        {{/title.text}}
      {{/card.title}}
      {{#card.content}}
        <p>
          The titles of Washed Out's breakthrough song and the first single from Paracosm share the
          The titles of Washed Out's breakthrough song and the first single from Paracosm share the
        </p>
      {{/card.content}}
      {{#card.actions class="layout-row"}}
        {{#paper-button}}Action 1{{/paper-button}}
        {{#paper-button}}Action 2{{/paper-button}}
      {{/card.actions}}
    {{/paper-card}}
  </div>
{{/each}}
</div>
-->
{{outlet}}
